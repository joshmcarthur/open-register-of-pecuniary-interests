<div class="container py-12 mx-auto px-4 sm:px-6 lg:px-8">
  <!-- Hero Section -->
  <div class="hero min-h-[500px] bg-gradient-to-br from-primary/10 via-base-200 to-secondary/10 rounded-2xl shadow-2xl">
    <div class="hero-content text-center">
      <div class="max-w-2xl">
        <div class="mb-6">
          <div class="badge badge-primary badge-lg mb-4">Transparency in Politics</div>
          <h1 class="text-6xl font-bold text-base-content leading-tight">
            Open Register of <span class="text-primary">Pecuniary</span> Interests
          </h1>
        </div>
        <p class="text-xl text-base-content/80 mb-8 leading-relaxed">
          A simple way to see what companies, properties and investments your elected officials have declared
        </p>

        <div class="w-full max-w-xxl mx-auto">
          <%= form_with url: interests_path, method: :get, html: { role: "search", class: "join w-full shadow-lg" } do |f| %>
            <%= f.search_field :q,
                placeholder: "Search by MP name, electorate, company, or interest type...",
                class: "input input-bordered join-item flex-1 text-lg h-14" %>
            <%= f.submit "Search", class: "btn btn-primary join-item h-14 px-8 text-lg" %>
          <% end %>
        </div>
      </div>
    </div>
  </div>


  <!-- CTA Cards Section -->
  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 my-12">
    <div class="card bg-base-100 shadow-xl hover:shadow-2xl transition-shadow duration-300">
      <div class="card-body">
        <div class="flex items-center gap-3 mb-3">
          <div class="p-3 rounded-full bg-primary/10">
            <%= render 'shared/icons/building', class: 'h-6 w-6 text-primary' %>
          </div>
          <h2 class="card-title text-lg">Browse Parliament</h2>
        </div>
        <p class="text-base-content/70">Current MPs and their declared interests</p>
        <div class="card-actions justify-end mt-4">
          <a href="<%= political_entities_path(jurisdiction: 'new-zealand-parliament') %>" class="btn btn-primary btn-sm">Browse</a>
        </div>
      </div>
    </div>

    <div class="card bg-base-100 shadow-xl hover:shadow-2xl transition-shadow duration-300">
      <div class="card-body">
        <div class="flex items-center gap-3 mb-3">
          <div class="p-3 rounded-full bg-secondary/10">
            <%= render 'shared/icons/dollar', class: 'h-6 w-6 text-secondary' %>
          </div>
          <h2 class="card-title text-lg">Explore Interests</h2>
        </div>
        <p class="text-base-content/70">Find who has stakes in specific companies</p>
        <div class="card-actions justify-end mt-4">
          <a href="<%= interests_path %>" class="btn btn-secondary btn-sm">Explore</a>
        </div>
      </div>
    </div>

    <div class="card bg-base-100 shadow-xl hover:shadow-2xl transition-shadow duration-300">
      <div class="card-body">
        <div class="flex items-center gap-3 mb-3">
          <div class="p-3 rounded-full bg-accent/10">
            <%= render 'shared/icons/chart', class: 'h-6 w-6 text-accent' %>
          </div>
          <h2 class="card-title text-lg">Compare Parties</h2>
        </div>
        <p class="text-base-content/70">Aggregate analysis across political parties</p>
        <div class="card-actions justify-end mt-4">
          <%= link_to "Compare", comparison_index_path, class: "btn btn-accent btn-sm" %>
        </div>
      </div>
    </div>

    <div class="card bg-base-100 shadow-xl opacity-50">
      <div class="card-body">
        <div class="flex items-center gap-3 mb-3">
          <div class="p-3 rounded-full bg-base-300">
            <%= render 'shared/icons/building', class: 'h-6 w-6 text-base-content/50' %>
          </div>
          <h2 class="card-title text-lg">Browse Councils</h2>
        </div>
        <p class="text-base-content/50">Local government representatives</p>
        <div class="badge badge-secondary mt-4">Coming soon!</div>
      </div>
    </div>
  </div>

  <!-- Stats Section -->
  <div class="stats stats-vertical sm:stats-horizontal shadow-lg w-full my-12 bg-base-100 border border-base-300 rounded-2xl">
    <div class="stat">
      <div class="stat-figure text-primary">
        <%= render 'shared/icons/users', class: 'inline-block w-8 h-8 stroke-current' %>
      </div>
      <div class="stat-title">Current MPs</div>
      <div class="stat-value text-primary"><%= HomepageStats.instance.mps_count %></div>
      <div class="stat-desc">Parliamentary representatives</div>
    </div>

    <div class="stat opacity-50">
      <div class="stat-figure text-secondary">
        <%= render 'shared/icons/dollar', class: 'inline-block w-8 h-8 stroke-current' %>
      </div>
      <div class="stat-title">Total Interests</div>
      <div class="stat-value text-secondary"><%= HomepageStats.instance.declared_interests_count %></div>
      <div class="stat-desc">Interests found</div>
    </div>

    <div class="stat">
      <div class="stat-figure text-accent">
        <%= render 'shared/icons/chart', class: 'inline-block w-8 h-8 stroke-current' %>
      </div>
      <div class="stat-title">Declared Interests</div>
      <div class="stat-value text-accent"><%= HomepageStats.instance.declared_interests_count %></div>
      <div class="stat-desc">Financial disclosures</div>
    </div>

    <div class="stat">
      <div class="stat-figure text-info">
        <%= render 'shared/icons/calendar', class: 'inline-block w-8 h-8 stroke-current' %>
      </div>
      <div class="stat-title">Latest Data</div>
      <div class="stat-value text-info"><%= HomepageStats.instance.latest_data_year %></div>
      <div class="stat-desc">Most recent register</div>
    </div>
  </div>

    <!-- Data Source Information -->
  <div class="bg-base-100 border border-base-300 rounded-lg p-6 my-8">
    <div class="flex items-start gap-3">
      <div class="flex-shrink-0 mt-1">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-base-content/60" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
        </svg>
      </div>
      <div class="flex-1">
        <h3 class="font-medium text-base-content mb-2">About This Data</h3>
        <p class="text-sm text-base-content/80 leading-relaxed">
          This information has been extracted from official parliamentary registers using AI technology (Gemini LLM).
          <strong>This data could not have been made publicly accessible without LLM assistance</strong> due to the complexity of processing thousands of pages of PDF documents.
        </p>
        <div class="mt-3 text-xs text-base-content/60 space-y-1">
          <p>• Each entry includes confidence scores and links to original source documents</p>
          <p>• Data is regularly updated as new registers are published</p>
          <p>• All information is sourced from official government publications</p>
        </div>
      </div>
    </div>
  </div>
</div>